
<div class="panel panel-default teacher_info" >
<div class="panel-heading">	
	<h5>Information Box</h5>
</div>
<div class="panel-body">

    <strong>Project:</strong> <%= get_selected_project.semester + ' ' + get_selected_project.year.to_s %>
    <% if get_selected_project.is_active %>
        (current)
	
    <% else %>
        (archived)
	
    <% end %>
    <% if get_selected_section == 'all' %>
        <strong>Sections:</strong> All
    <% else %>
        <strong>Section:</strong> <%= get_selected_section %>
    <% end %>
    <br /><strong>Current User:</strong> <%= @current_user.last_name  || @current_user.school_id%>

</div>
</div>
