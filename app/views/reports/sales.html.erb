<%= render :partial => 'dropdown' , :locals => {:sections => @sections}%>

<% provide(:title, 'Sales') %>
<% provide(:excel, (link_to (image_tag 'excel.png'), reports_sales_path(format: "xls"), :title => "Export to Excel").html_safe) %>



<div id = "section_refresh">  
<%= render :partial => 'sales'%>
</div>
