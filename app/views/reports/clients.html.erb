<% provide(:title, 'Client Master List') %>
<% provide(:excel, (link_to (image_tag 'excel.png'), reports_clients_path(format: "xls"), :title => "Export to Excel").html_safe) %>

<%= render :partial => 'clients'%>