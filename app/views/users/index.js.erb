$("#section_refresh").html("<%=j render partial: 'users/manage_section', locals: { current_students: @current_students,
	                                                  selected_section: @selected_section } %>")
//alert("this is working!");
