
<% provide(:title, 'Editing User') %>

<% if @user.role != 3 %>
	<%= render 'form' %>
<% else %>
	<%= render 'teacher_form' %>
<% end %>

<%= link_to 'Show', @user, :class => 'btn btn-large btn-primary btn-sm' %> |
<%= link_to 'Back to Manage Section', users_path, :class => 'btn btn-large btn-primary btn-sm' %>

<br /><br /><br /><br /><br /><br /><br /><br /><br />
