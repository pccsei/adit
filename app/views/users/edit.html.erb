<% if @user.role != 3 %>
    <% provide(:title, 'Editing User') %>
	<%= render 'form' %>
	<%= link_to 'Back to Manage Section', users_path %>
<% else %>
    <% provide(:title, 'Editing Teacher') %>
	<%= render 'teacher_form' %>
	<%= link_to "Back to Teacher's Page", users_teachers_path %>
<% end %>
